
# Configure the TanStack Query cache TTL in milliseconds
NEXT_PUBLIC_QUERY_CACHE_TTL_MS=300000

# REST Countries API key for server-side fetches. Uses rc_live_demo when unset.
REST_COUNTRIES_API_KEY=
